# Зависимости приложения. Дублируют секцию [project.dependencies]
# в pyproject.toml, который остаётся источником метаданных проекта.
# Установка: pip install -r requirements.txt

customtkinter>=5.2.0
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0
requests>=2.31.0
beautifulsoup4>=4.12.0
numpy>=1.24.0
